接口的default方法，默认方法可以给接口实现一些函数特性

#流由三部分构成：
1、源
2、0-n个中间操作
3、终止操作

#流的操作分类：
1、惰性求值（中间操作）
2、及早求值（终止操作）

#流的创建
1、Stream.of(T... t)
2、Arrays.stream(T... t)
3、Collection.stream()
4、Stream.of()
5、Stream.iterator().limit()...无限串行流
6、Stream.empty()

#Stream采用内部迭代，相当于迭代的功能是stream自身与生俱来的，而一系列的操作都会被加到自身迭代器中被执行。
所以一旦有终止操作，迭代器则在内部对每一个元素进行串行操作，无论有多少操作都只迭代一次。
这样就可以并行，并且避免并发问题。

#集合关注的是：数据与数据存储本身
#流关注的是：对数据的计算
