package redis;

/**
 * @author:luojin
 * @apiNote:
 * @since: 2020-10-28 10:27
 */
public class Redis {
    /**
     * 一、redis传统五大数据类型的落地应用
     * 1、string（字符）
     * 2、list（列表）==>落地实现：
     * 3、hash（散列）==>java中Map<String,Map<Object,Object>>,落地：购物车
     * 4、set（集合）==>抽奖、朋友圈点赞，微博好友社交共同关注（交集），关注推荐（差集）
     * 5、zset(sorted set，有序集合)==>投票实时排序
     * 除了上述5大类型，还知道哪些类型
     * 6、bitmap（位图）
     * 7、HyperLogLog（统计）
     * 8、GEO（地理）
     * 9、Stream（流）
     */
}
